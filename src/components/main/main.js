import React from 'react';
import { ItemListContainer } from '../itemListContainer/itemListContainer';

export const Main = () => {
    return (
        <main>
            <ItemListContainer />
        </main>
    );
} 